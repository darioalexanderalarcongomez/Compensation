@model Compensation.Models.Payment

@{
    ViewData["Title"] = "Delete";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-8">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-danger text-white fw-bold">Delete Payment</div>
            <div class="card-body">
                <h5 class="mb-3">Are you sure you want to delete this?</h5>
                <dl class="row mb-4">
                    <dt class="col-sm-4">@Html.DisplayNameFor(model => model.PaymentOrder)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.PaymentOrder)</dd>
                    <dt class="col-sm-4">@Html.DisplayNameFor(model => model.TypePayment)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.TypePayment.TypePayment_Id)</dd>
                    <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Event)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Event.Event_Id)</dd>
                </dl>
                <form asp-action="Delete" class="d-flex justify-content-between">
                    <input type="hidden" asp-for="Payment_Id" />
                    <input type="submit" value="Delete" class="btn btn-danger px-4" />
                    <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
                </form>
            </div>
        </div>
    </div>
</div>
